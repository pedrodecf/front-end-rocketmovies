import styled from "styled-components"

export const Container = styled.div`
  width: fit-content;
  height: 2.4rem;
  background-color: var(--tags-1);
  color: var(--white);
  font-family: "Roboto", sans-serif;
  font-size: 1.2rem;
  font-weight: 400;
  display: flex;
  justify-content: center;
  align-items: center;
  padding: 0 1.6rem;
  border-radius: 0.8rem;
  cursor: pointer;
`
